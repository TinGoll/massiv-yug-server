import { WebsocketExceptionsFilter } from './websocket-exceptions.filter';

describe('WebsocketExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new WebsocketExceptionsFilter()).toBeDefined();
  });
});
